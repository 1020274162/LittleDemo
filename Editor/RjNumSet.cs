using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RjNumSet : MonoBehaviour
{
    public static int NumT;
    [Header("递增数字")]
    public int Num;
    
    // Start is called before the first frame update
    void Start()
    {
        NumT=Num;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
